---
status: unfilled
captured:
volatility: high
review_after:
---

# Benchmark — where you are now

> Filled by the `benchmark-me` skill. This is the file that goes stale fastest;
> treat anything older than three months as a claim, not a fact.

## Evidence reviewed

What the agent actually looked at, and when. A benchmark built only from self-report
is worth very little — the point of naming sources here is that the next run can
re-read them rather than re-ask.

| Source | Type | Reviewed |
| --- | --- | --- |
| *github.com/user* | *public repos* | *2026-08-08* |
| *user.dev* | *portfolio site* | *2026-08-08* |
| *CV / résumé* | *supplied file* | *2026-08-08* |

## Capability positions

One row per capability area from `spec.md`. **Position is inferred from evidence
where evidence exists**, and marked as self-reported where it does not — the two are
not the same and the report must not blur them.

| Area | Position | Basis | Evidence |
| --- | --- | --- | --- |
| *MCP* | *Strong* | *evidence* | *six MCP servers in public repos* |
| *Evaluation and error analysis* | *Weak* | *evidence* | *no test directories across the tree* |
| *TypeScript* | *Working* | *self-reported* | *—* |

Positions: **Strong** / **Working** / **Weak** / **None**. Resist finer grain; the
scorecard does not need it and false precision here propagates into the report.

## Credentials already held

<!-- Existing certifications, degrees, published work. Affects prerequisites and
     whether a given credential adds any signal the user does not already have. -->

## Notable absences

The sharp, specific gaps — the ones that are narrow and fixable rather than "needs
more experience". These feed the *Where to invest* section of the report.

<!-- Be concrete. "No CI-invoked agents anywhere in the repo tree" is useful.
     "Could be stronger on DevOps" is not. -->
